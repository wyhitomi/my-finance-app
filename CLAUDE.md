# CLAUDE.md

Siga integralmente [`AGENTS.md`](AGENTS.md). Resumo do que nunca fazer:

- implementar sem ler os ADRs (`docs/adr/`) e RFCs (`docs/rfc/`) relevantes;
- implementar sem testes (TDD — teste falhando primeiro);
- começar uma feature sem proposta OpenSpec revisada (`/opsx:propose`).

Comandos OpenSpec disponíveis: `/opsx:propose`, `/opsx:explore`, `/opsx:apply`, `/opsx:update`, `/opsx:sync`, `/opsx:archive`.
